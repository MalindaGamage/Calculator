services:
  - type: web
    name: calculator-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://calculator-YOUR_NAME.vercel.app
    healthCheckPath: /api/history
